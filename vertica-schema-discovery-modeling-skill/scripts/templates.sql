-- templates.sql
-- Minimal snippets Cline can reuse in planning (load only when needed)

-- Count rows
-- SELECT COUNT(*) AS rows FROM "schema"."table";

-- Column profiling (approx)
-- SELECT
--   COUNT(*) AS rows,
--   COUNT(DISTINCT col) AS distinct_vals
-- FROM "schema"."table";
